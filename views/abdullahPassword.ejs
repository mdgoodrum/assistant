<!DOCTYPE html>
<html>
<head>
  <style>
    #myVideo {
      position: fixed;
      right: 0;
      bottom: 0;
      min-width: 100%; 
      min-height: 100%;
    }
  </style>
 <script src="//cdnjs.cloudflare.com/ajax/libs/annyang/2.6.0/annyang.min.js"></script>
   <script>
     if (annyang) {
       // Let's define a command.
       var commands = {
         'west mar': function() { 
            location.replace('/abdullah');
          },
          'log out': function() {
            location.replace('/logout');
          }
       };
       // This should be true
       console.log(annyang.isListening())
       // Add our commands to annyang
       annyang.addCommands(commands);
       // Start listening.
       annyang.start();
       annyang.addCallback('resultNoMatch', function(phrases) {
        location.replace('/abdullahInvalid');
       });
     }
    </script>
  </head>
  <body>
    <video id="myVideo" autoplay>
      <source src="http://localhost:3000/password" type="video/webm">
    </video>
  </body>
</html>